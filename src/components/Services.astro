---
import ServiceCard from './ServiceCard.astro';
import { services } from '../data/services';
---

<section class="max-w-7xl mx-auto px-4 sm:px-5 lg:px-8 py-16">
  <h2 class="text-2xl text-center md:text-5xl font-bold mb-4 text-black mb-5">Servicios</h2>
  <p class="text-sm text-center md:text-xl mb-10 text-black mb-5 text-gray-700 text-balance">Asesoramiento automotriz especializado en compra y venta de vehículos
    con mas de 10 años de experiencia en el sector.
  </p>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mt-10">
    {services.map((service) => (
      <ServiceCard {...service} />
    ))}
  </div>
</section>